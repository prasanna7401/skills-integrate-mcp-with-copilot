# Search and paginated listing endpoints

Implement search endpoints (by admission number, name) and paginated list endpoints for activities and students. Support filtering by activity, semester, and branch.

**Acceptance criteria**
- `GET /students?name=...&page=...&per_page=...` with filters
- `GET /activities?filter=...&page=...`
- Documentation in README for how to use filters
