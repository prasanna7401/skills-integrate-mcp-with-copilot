# Faculty assignment and management

Add models and endpoints to assign faculty to students or activities, list faculty members, and allow faculty to view their assigned students and signups.

**Acceptance criteria**
- Faculty user model and CRUD endpoints
- `POST /students/{id}/assign-faculty` endpoint
- `GET /faculty/{id}/students` endpoint
